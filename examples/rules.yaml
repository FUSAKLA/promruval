groups:
  - name: group1
    partial_response_strategy: abort
    rules:
      - record: recorded_metrics
        expr: 1
        labels:
          foo: bar

  - name: testGroup
    rules:
      - alert: test
        expr: avg_over_time(max_over_time(up{job="prometheus"}[10h] offset 5m)[10m:10m])
        labels:
          severity: critical
          team: sre@mail.com
          page: true
        annotations:
          title: test alert
          playbook: http://foo.bar/nonexisting/playbook
          disabled_validation_rules: check-team-label,check-prometheus-limitations
